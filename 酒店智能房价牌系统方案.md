# 酒店智能房价牌系统方案

## 一、项目背景

酒店大厅现有一台竖屏电视，希望将其改造为智能房价牌，用于展示酒店宣传图片、房型价格、优惠活动及相关信息，提高酒店形象并减少传统纸质房价牌的维护成本。

服务端部署在家庭服务器，单位展示端与管理端通过域名远程访问。

---

# 二、项目目标

实现一套稳定、易维护、可扩展的酒店数字房价牌系统。

主要功能：

- 酒店图片自动轮播
- 实时展示房价
- 后台在线修改价格
- 自动刷新显示内容
- 显示酒店公告
- 显示时间与天气
- 支持后续扩展房态及OTA价格同步

---

# 三、系统架构

```text
┌─────────────────┐
│    管理后台      │
│  (任意浏览器)    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    家庭服务器    │
│ FastAPI + SQLite │
│  + 前端静态文件   │
└────────┬────────┘
         │ 域名 / HTTPS
         ▼
┌─────────────────┐
│ 单位展示端浏览器  │
│ (Windows kiosk) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  竖屏电视展示    │
└─────────────────┘
```

---

# 四、硬件方案

## 显示设备

- 酒店现有竖屏电视
- 分辨率建议：
  - 1080 × 1920
  - 2160 × 3840（4K）

## 展示端设备

使用单位现有 Windows 电脑（与轻音乐系统同机），无需新购设备：

- 仅需运行浏览器（kiosk 模式），无需安装后端服务
- 内存 8GB 即可满足：Windows 3～4GB + 浏览器 0.5～1GB + 轻音乐系统 0.5GB
- 若现有设备内存低于 8GB，建议加装内存

## 服务端

部署在家庭服务器，由用户自行管理，本方案不涉及服务器硬件采购。

---

# 五、软件架构

## 前端

技术栈：

- React
- TailwindCSS
- Vite

功能：

- 房价展示页面
- 图片轮播页面
- 管理后台界面

---

## 后端

技术栈：

- FastAPI

功能：

- 提供管理接口
- 提供展示数据接口
- 文件上传管理

---

## 数据库

技术栈：

- SQLite

特点：

- 单酒店场景足够使用
- 无需独立数据库服务器
- 备份简单

---

# 六、展示界面设计

## 页面比例

设计尺寸：

```text
1080 × 1920
```

适用于所有竖屏电视。

页面固定按 1080×1920 设计，任意窗口下自动等比缩放居中：桌面横屏浏览器预览时左右留黑边，电视全屏时为 1:1 像素级显示。

---

## 分辨率适配

设计稿按 1080 × 1920 制作，兼容 4K 电视（2160 × 3840）：

- 文字使用矢量渲染，任意分辨率下保持清晰
- 上传图片建议分辨率不低于 1920 × 1080，确保 4K 下不模糊
- 部署前确认现场电视实际分辨率

---

## 防烧屏

房价信息区域为长期静态内容，需防止屏幕残影：

- 图片轮播区域内容持续变化，风险低
- 静态文字区域做周期性微小像素偏移
- 如后续更换 OLED 屏，此措施为必需

---

## 页面布局

酒店图片均为横版，图片轮播区采用 16:9 条幅展示，其余空间留给房价与公告：

```text
┌─────────────────────┐
│ 酒店名        时间   │
│              天气    │
├─────────────────────┤
│                     │
│    图片轮播区        │
│    (16:9 条幅)      │
│                     │
├─────────────────────┤
│     今日房价         │
│                     │
│ 标准大床房 ¥268 会员价 ¥168 │
│ 豪华大床房 ¥298 会员价 ¥198 │
│ 商务双床房 ¥318 会员价 ¥218 │
│ 家庭房     ¥358 会员价 ¥258 │
│                     │
├─────────────────────┤
│ 入住时间：14:00以后 │
│ 退房时间：12:00以前 │
│ 早餐时间：07:00-09:30│
└─────────────────────┘
```

---

# 七、功能设计

## 1. 图片轮播

支持上传：

- 酒店外观
- 大堂
- 客房
- 早餐厅
- 停车场
- 周边景点

功能：

- 自动轮播
- 自动缩放
- 支持排序
- 支持删除

轮播间隔：

```text
5 秒
```

---

## 2. 房价管理

每个房型维护两个价格：

| 房型 | 门市价 | 会员价 |
|--------|--------|--------|
| 标准大床房 | 268 | 168 |
| 豪华大床房 | 298 | 198 |
| 商务双床房 | 318 | 218 |
| 家庭房 | 358 | 258 |

展示方式：

- 门市价以删除线小字展示
- 会员价以大号金色数字突出展示

特点：

- 实时修改
- 自动保存
- 自动刷新展示

---

### 满房状态

支持标记某房型为"满房"：

```text
标准大床房  满房
```

展示端显示"满房"而非价格，避免误导客人。

---

### 价格修改日志

记录每次价格变更：

| 字段 | 说明 |
|--------|--------|
| 修改时间 | 精确到秒 |
| 房型 | 涉及房型 |
| 原价格 | 修改前价格 |
| 新价格 | 修改后价格 |

便于追溯，应对财务纠纷。

---

## 3. 公告管理

支持：

```text
入住时间：14:00以后

退房时间：12:00以前

早餐时间：07:00-09:30
```

也支持营销信息：

```text
连住两晚95折

会员入住赠早餐
```

---

## 4. 时间显示

显示：

```text
2026-08-13

星期四

19:30
```

自动更新。

时间从后端服务器获取，避免电视长时间运行后时钟漂移。

---

## 5. 天气显示

显示：

```text
晴

29℃
```

数据来源：

- 首选：和风天气 API（个人可申请，免费版每日约 1000 次调用，单屏足够）
- 备选：高德天气 API（需企业资质）

容错处理：

- 天气接口失败时显示最近一次缓存数据
- 断网时优雅跳过天气模块，不影响其他内容

---

# 八、后台管理功能

## 房价管理

```text
房型管理

价格维护

排序调整
```

---

## 图片管理

```text
上传图片

删除图片

调整顺序
```

---

## 公告管理

```text
编辑公告

设置活动信息
```

---

## 系统设置

```text
轮播时间设置

酒店名称设置

LOGO上传
```

---

## 登录认证

后台管理必须登录后才能操作：

- 账号密码登录
- 防止酒店 WiFi 内任意设备篡改价格、删除图片

---

# 九、自动刷新机制

展示页每30秒自动同步一次数据。

流程：

```text
后台修改价格
      ↓
保存数据库
      ↓
展示页面获取新数据
      ↓
电视自动更新
```

无需人工刷新。

## 跨网络访问优化

展示端通过公网访问家庭服务器，需考虑带宽与稳定性：

- 轮播图片：浏览器缓存 + 预加载，首次加载后不再重复下载
- 数据同步：仅传输 JSON 数据，30 秒一次，流量极小
- 断网容错：网络中断时展示端保留最后数据继续显示，恢复后自动同步

---

# 十、部署方案

## 总体架构

```text
家庭服务器（服务端）
  ├── FastAPI 后端 + SQLite + 前端静态文件
  └── 通过域名对外提供访问（部署细节用户自理）

单位展示端（客户端）
  └── Windows 电脑浏览器 kiosk 模式访问展示地址

管理后台
  └── 任意浏览器访问后台地址
```

服务端部署细节由用户自行管理，本方案仅约束客户端。

---

## 访问安全

后台暴露于公网，必须做好防护：

- 全站启用 HTTPS（Let's Encrypt 免费证书）
- 后台登录使用强密码
- 建议限制后台访问来源（IP 白名单或内网穿透工具，如 Tailscale / frp）
- 展示地址可公开访问

---

## 后台地址

```text
https://域名/admin
```

---

## 展示地址

```text
https://域名/display
```

---

## 展示端开机自启动（单位 Windows 设备）

### BIOS 设置

```text
通电自动开机（Restore on AC Power Loss = Power On）
```

### Windows 设置

```text
自动登录（免密码进入桌面）
任务计划程序：开机启动 kiosk 浏览器
电源选项：永不睡眠、永不关闭显示器
```

### Windows Update 策略

```text
暂停/延迟自动更新
禁止更新后自动重启
```

> Windows 更新强制重启是 24×7 展示设备最常见的故障来源，必须处理。

确保断电恢复后自动运行。

---

# 十一、运维方案

## 数据备份（服务端）

- 服务端由用户管理，SQLite 数据库建议每天定时备份
- 备份文件同步至网盘或 U 盘，保留最近 30 天

## 展示端远程运维

- 单位 Windows 设备安装 ToDesk / 向日葵
- 远程排查展示问题，无需到现场

## 展示端监控与自愈

- PowerShell 看门狗脚本，任务计划程序定时执行
- 检测展示页浏览器进程，异常退出自动拉起
- 断网恢复后浏览器自动重连（展示页内置重试机制）

## 浏览器 Kiosk 模式

展示页使用 Edge / Chrome `--kiosk` 模式运行：

- 自动全屏，无地址栏
- 无更新弹窗等干扰
- 断电恢复后自动进入展示
- 备选：Windows 分配访问权限（Assigned Access）官方 kiosk 方案

## 与轻音乐系统共存

- 房价牌系统全程无声，无音频冲突
- 轻音乐系统可正常前台/后台播放
- 浏览器关闭硬件加速，进一步降低 GPU 与内存占用

---

# 十二、后续扩展规划

## 房态显示

```text
标准大床房

剩余 3 间
```

---

## OTA价格同步

支持接入：

- 携程
- 美团
- 飞猪

实现价格自动同步。

> 注：携程/美团/飞猪官方 API 需 PMS 系统对接资质，单店难以申请。
> 现实方案：先从 OTA 后台导出房价表导入本系统，或人工同步；待具备 PMS 资质后再接入官方 API。

---

## 会议欢迎屏

```text
欢迎参加

XX公司年度会议
```

---

## 婚宴迎宾屏

```text
欢迎参加

张先生 & 李女士

新婚典礼
```

---

## 多门店管理

支持：

```text
酒店A

酒店B

酒店C
```

统一后台管理。

架构演进路径：

- 当前：单机部署（后端 + 前端 + SQLite 同机）
- 多门店：后端迁至云服务器，数据库迁移至 MySQL/PostgreSQL，各门店仅保留展示端
- 展示端无需改动，后台升级为多门店数据隔离

---

# 十三、项目优势

- 替代传统纸质房价牌
- 提升酒店品牌形象
- 房价修改实时生效
- 无需频繁维护
- 成本低
- 可持续扩展
- 支持多门店统一管理
- 支持未来接入酒店管理系统（PMS）

---

# 十四、推荐实施路线

## 第一阶段（1周）

完成：

- 图片轮播
- 房价展示
- 管理后台
- 后台登录认证
- 自动刷新

验收标准：

- 后台改价后 30 秒内电视生效
- 断电重启后 3 分钟内自动恢复展示

上线使用。

---

## 第二阶段（2周）

增加：

- 天气显示
- 活动公告
- LOGO管理
- 数据备份
- 远程运维工具
- 看门狗监控

---

## 第三阶段（后续）

增加：

- 房态显示
- OTA价格同步
- 多门店管理
- 会议欢迎屏

逐步升级为完整酒店数字标牌系统。